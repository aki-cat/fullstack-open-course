{
  "name": "phonebook",
  "version": "1.0.0",
  "private": true,
  "main": "src/index.js",
  "scripts": {
    "start": "node .",
    "dev": "nodemon .",
    "build:frontend": "rm -rf dist/ && cd ../part2/phonebook && npm run build && cp -rv ./dist ../../part3/",
    "deploy": "fly deploy",
    "deploy:full": "npm run build:frontend && npm run deploy",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "express": "^4.21.2",
    "morgan": "^1.10.0",
    "nodemon": "^3.1.9"
  },
  "devDependencies": {
    "@flydotio/dockerfile": "^0.7.10"
  }
}
